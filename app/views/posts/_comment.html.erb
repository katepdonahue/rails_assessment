<p>
  <h3>New Comment</h3>
  <%= form_for(@comment, {:url => "/posts/#{@post.id}/comments"}) do |f| %>
    <%= f.label :body%>
    <br>
    <%= f.text_area :body%>
    <br>
    <%= f.submit :save %>
  <% end %>
</p>